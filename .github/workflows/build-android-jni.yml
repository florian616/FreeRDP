name: build-android-jni

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Linux build deps (incl. nasm)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ninja-build make autoconf automake libtool pkg-config \
            nasm

      - name: Install NDK 26.1.10909125
        run: |
          sdkmanager --install "ndk;26.1.10909125"
          echo "ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/26.1.10909125" >> $GITHUB_ENV

      - name: Install CMake 3.22.1 and remove CMake 4.x
        run: |
          sdkmanager --install "cmake;3.22.1"
          sdkmanager --uninstall "cmake;4.1.2" || true
          echo "$ANDROID_SDK_ROOT/cmake/3.22.1/bin" >> $GITHUB_PATH

      - name: Verify toolchain
        run: |
          set -eux
          which cmake
          cmake --version
          which nasm
          nasm -v
          echo "NDK at: $ANDROID_NDK_HOME"

      - name: Build FreeRDP native libs (arm64-v8a)
        env:
          SDK: ${{ env.ANDROID_SDK_ROOT }}
          NDK: ${{ env.ANDROID_NDK_HOME }}
        run: |
          set -eux
          export PATH="$ANDROID_SDK_ROOT/cmake/3.22.1/bin:$PATH"
          cmake --version

          # Wir starten das offizielle Build-Script.
          # Auch wenn es intern noch Dependencies für x86 baut, klappt es jetzt (nasm ist da).
          bash scripts/android-build-freerdp.sh \
            --sdk "$SDK" \
            --ndk "$NDK" \
            --arch "arm64-v8a" \
            --target 23 \
            --conf scripts/android-build-release.conf

      - name: Upload jniLibs
        uses: actions/upload-artifact@v4
        with:
          name: jniLibs
          path: client/Android/Studio/freeRDPCore/src/main/jniLibs
